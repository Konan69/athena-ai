# prefer hoisted node_modules layout for editor & tooling compatibility
node-linker=hoisted

# use workspace protocol resolution for dependencies like "workspace:*"
auto-install-peers=false

# optional: speed up installs by enabling pnpm store in repo (choose local to cache on CI)
store-dir=.pnpm-store

# save exact versions by default (optional)
save-exact=true
